import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary seed — Tailwind Indigo 500, vibrant but professional
  static const Color seedColor = Color(0xFF6366F1);

  /// Accent used for decorative highlights (accent bar on section titles, etc.)
  static const Color accentLight = Color(0xFF4F46E5); // deeper indigo in light
  static const Color accentDark = Color(0xFF818CF8);  // soft lavender in dark
}
